#pragma once
#include <glm/vec3.hpp>
#include <array>

namespace pbd {
	class Engine;

	// This constraint would also be very expensive. Effectively prevents stretching of the material.
	struct IsoBend {

	};
}